% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct-snap-to-roads.R
\name{snap_to_roads}
\alias{snap_to_roads}
\title{Snap Points to Roads}
\usage{
snap_to_roads(
  points,
  travel_mode = NULL,
  point_properties = c("oid_routing_streets", "posted_speed_limit_kph",
    "posted_speed_limit_mph", "posted_speed_limit_mps", "posted_truck_speed_limit_kph",
    "posted_truck_speed_limit_mph", "posted_truck_speed_limit_mps"),
  line_properties = c("oid_routing_streets", "length_kilometers", "length_miles",
    "posted_speed_limit_kph", "posted_speed_limit_mph", "posted_speed_limit_mps",
    "posted_truck_speed_limit_kph", "posted_truck_speed_limit_mph",
    "posted_truck_speed_limit_mps"),
  analysis_region = c("europe", "japan", "korea", "middleastandafrica", "northamerica",
    "southamerica", "southasia", "thailand"),
  return_lines = TRUE,
  token = arc_token()
)
}
\arguments{
\item{points}{An \code{sf} or \code{sfc} object containing the point geometries to snap
to the road network. Recognized attribute columns such as \code{track_id} and
\code{location_timestamp} are used when present.}

\item{travel_mode}{Character. The name of the travel mode to use. See
\code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{point_properties}{Character vector. Road properties returned on the
snapped points output. One or more of: \code{"oid_routing_streets"},
\code{"posted_speed_limit_kph"}, \code{"posted_speed_limit_mph"},
\code{"posted_speed_limit_mps"}, \code{"posted_truck_speed_limit_kph"},
\code{"posted_truck_speed_limit_mph"}, \code{"posted_truck_speed_limit_mps"}.}

\item{line_properties}{Character vector. Road properties returned on the
output lines. One or more of: \code{"oid_routing_streets"},
\code{"length_kilometers"}, \code{"length_miles"}, \code{"posted_speed_limit_kph"},
\code{"posted_speed_limit_mph"}, \code{"posted_speed_limit_mps"},
\code{"posted_truck_speed_limit_kph"}, \code{"posted_truck_speed_limit_mph"},
\code{"posted_truck_speed_limit_mps"}. Ignored unless \code{return_lines = TRUE}.}

\item{analysis_region}{Character. Region in which to perform the analysis. One
of: \code{"europe"}, \code{"japan"}, \code{"korea"}, \code{"middleastandafrica"},
\code{"northamerica"}, \code{"southamerica"}, \code{"southasia"}, \code{"thailand"}. Default:
auto-detected from the location of \code{points}.}

\item{return_lines}{Logical. Whether to return output lines representing the
roads traversed. Default: \code{TRUE}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A named list:
\itemize{
\item \code{snapped_points}: the input points snapped to the road network
\item \code{snap_lines}: the traversed road segments (only when \code{return_lines = TRUE})
\item \code{usage_cost}: list with \code{numObjects} and \code{credits} used
\item \code{messages}: status and warning messages from the service
}
}
\description{
Snaps a set of GPS points to the most likely roads traveled and optionally
returns the traversed road segments as lines using the ArcGIS \verb{/SnapToRoads}
service.
}
\examples{
\dontrun{
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

pts <- st_sfc(
  st_point(c(-122.43410, 37.80016)),
  st_point(c(-122.43460, 37.80074)),
  st_point(c(-122.43539, 37.80096)),
  crs = 4326
)

snap_to_roads(pts)
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/snap-to-roads-direct/}{API Reference}
}
\seealso{
Other direct: 
\code{\link{find_closest_facilities}()},
\code{\link{find_routes}()},
\code{\link{find_service_areas}()},
\code{\link{od_cost_matrix}()},
\code{\link{route_vehicles}()}
}
\concept{direct}
