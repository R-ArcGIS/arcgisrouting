% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct-solve-vrp.R
\name{route_vehicles}
\alias{route_vehicles}
\title{Route Vehicles}
\usage{
route_vehicles(
  orders,
  depots,
  routes = NULL,
  travel_mode = NULL,
  locate_settings = NULL,
  analysis_region = NULL,
  time_zone_usage_for_time_fields = NULL,
  default_date = NULL,
  breaks = NULL,
  time_units = NULL,
  distance_units = NULL,
  time_window_factor = NULL,
  spatially_cluster_routes = TRUE,
  route_zones = NULL,
  route_renewals = NULL,
  order_pairs = NULL,
  excess_transit_factor = NULL,
  uturn_policy = NULL,
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  use_hierarchy_in_analysis = TRUE,
  restrictions = NULL,
  attribute_parameter_values = NULL,
  time_impedance = NULL,
  distance_impedance = NULL,
  impedance = NULL,
  populate_route_lines = TRUE,
  route_line_simplification_tolerance = NULL,
  populate_directions = FALSE,
  directions_language = NULL,
  directions_style_name = NULL,
  save_route_data = FALSE,
  save_output_layer = FALSE,
  populate_stop_shapes = FALSE,
  ignore_invalid_order_locations = FALSE,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{orders}{an \code{sf} or \code{sfc} object containing point geometries representing locations to visit.}

\item{depots}{an \code{sf} or \code{sfc} object containing point geometries representing depot locations.}

\item{routes}{default \code{NULL}. A \code{data.frame} describing vehicle and driver characteristics.}

\item{travel_mode}{Character. The name of the travel mode to use. See
\code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{locate_settings}{default \code{NULL}. A list controlling how inputs are located on the network.}

\item{analysis_region}{default \code{NULL}. A scalar character. One of \code{"europe"}, \code{"japan"},
\code{"korea"}, \code{"middle_east_and_africa"}, \code{"north_america"}, \code{"south_america"},
\code{"south_asia"}, \code{"thailand"}. Speeds up analysis when specified.}

\item{time_zone_usage_for_time_fields}{default \code{NULL}. A scalar character. One of
\code{"geo_local"} or \code{"utc"}. Time zone used for all date-time input fields.}

\item{default_date}{default \code{NULL}. A \code{POSIXt} or character scalar. The date on which
all routes start. Only the date portion is used.}

\item{breaks}{default \code{NULL}. A \code{data.frame} of rest period definitions for routes.}

\item{time_units}{default \code{NULL}. A scalar character. One of \code{"seconds"}, \code{"minutes"},
\code{"hours"}, \code{"days"}. Units for all time-based attribute values.}

\item{distance_units}{default \code{NULL}. A scalar character. One of \code{"miles"},
\code{"kilometers"}, \code{"meters"}, \code{"feet"}, \code{"yards"}, \code{"nautical_miles"}. Units for all
distance-based attribute values.}

\item{time_window_factor}{default \code{NULL}. A scalar character. One of \code{"low"},
\code{"medium"}, \code{"high"}. Importance of honoring time windows.}

\item{spatially_cluster_routes}{default \code{TRUE}. A logical scalar. Whether orders
assigned to a route are spatially clustered.}

\item{route_zones}{default \code{NULL}. An \code{sf} or \code{sfc} polygon object delineating work
territories for routes.}

\item{route_renewals}{default \code{NULL}. A \code{data.frame} of intermediate depot renewal
locations for routes.}

\item{order_pairs}{default \code{NULL}. A \code{data.frame} pairing pickup and delivery orders.}

\item{excess_transit_factor}{default \code{NULL}. A scalar character. One of \code{"low"},
\code{"medium"}, \code{"high"}. Importance of reducing excess transit time for order pairs.}

\item{uturn_policy}{default \code{NULL}. A scalar character. One of \code{"allow_uturn"},
\code{"allow_dead_ends_and_intersections_only"}, \code{"allow_dead_ends_only"}, \code{"no_uturns"}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object. Default: \code{NULL}.}

\item{use_hierarchy_in_analysis}{default \code{TRUE}. A logical scalar. Whether to use
the street network hierarchy when finding routes.}

\item{restrictions}{Character vector. Restriction names to honor.
Default: \code{NULL}.}

\item{attribute_parameter_values}{default \code{NULL}. A list of objects providing
additional values required by an attribute or restriction.}

\item{time_impedance}{default \code{NULL}. A scalar character. Time-based impedance.
One of \code{"travel_time"}, \code{"minutes"}, \code{"walk_time"}, \code{"truck_minutes"},
\code{"truck_travel_time"}.}

\item{distance_impedance}{default \code{NULL}. A scalar character. Distance-based
impedance. One of \code{"miles"}, \code{"kilometers"}.}

\item{impedance}{default \code{NULL}. A scalar character. The impedance type. One of
\code{"travel_time"}, \code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"},
\code{"walk_time"}, \code{"miles"}, \code{"kilometers"}.}

\item{populate_route_lines}{default \code{TRUE}. A logical scalar. Whether output routes
include the exact street shape.}

\item{route_line_simplification_tolerance}{default \code{NULL}. A numeric scalar.
Simplification tolerance for route geometry.}

\item{populate_directions}{default \code{FALSE}. A logical scalar. Whether to generate
driving directions for each route.}

\item{directions_language}{default \code{NULL}. A scalar character. Language code for
directions (e.g. \code{"en"}).}

\item{directions_style_name}{default \code{NULL}. A scalar character. One of \code{"desktop"},
\code{"navigation"}, \code{"campus"}.}

\item{save_route_data}{default \code{FALSE}. A logical scalar. Whether to save results
as a \code{.zip} file geodatabase.}

\item{save_output_layer}{default \code{FALSE}. A logical scalar. Whether to save the
analysis as a network analysis layer package.}

\item{populate_stop_shapes}{default \code{FALSE}. A logical scalar. Whether output stops
include point geometries.}

\item{ignore_invalid_order_locations}{default \code{FALSE}. A logical scalar. Whether to
ignore invalid orders instead of failing.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A named list:
\itemize{
\item \code{unassigned_stops}: orders that could not be assigned to any route
\item \code{stops}: assigned stop features with route and sequence information
\item \code{routes}: route features with geometry and cost attributes
\item \code{directions}: driving directions (only when \code{populate_directions = TRUE})
\item \code{solve_succeeded}: logical indicating whether the solve completed
\item \code{usage_cost}: list with \code{numObjects} and \code{credits} used
\item \code{messages}: status and warning messages from the service
}
}
\description{
Solves a vehicle routing problem (VRP) to find the most effective routes
for a fleet of vehicles visiting a set of orders.
}
\examples{
\dontrun{
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

orders <- st_sf(
  name = c("Order 1", "Order 2"),
  geometry = st_sfc(
    st_point(c(-0.1891, 51.5254)),
    st_point(c(-0.1744, 51.5353)),
    crs = 4326
  )
)

depots <- st_sf(
  name = "Depot1",
  geometry = st_sfc(st_point(c(-0.2, 51.5)), crs = 4326)
)

routes <- data.frame(
  name = "Truck1",
  start_depot_name = "Depot1",
  end_depot_name = "Depot1",
  capacities = "40000"
)

result <- route_vehicles(orders, depots, routes)
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/vehicle-routing-problem-service-direct/}{API Reference}
}
\seealso{
Other direct: 
\code{\link{find_closest_facility}()},
\code{\link{find_routes}()},
\code{\link{find_service_areas}()},
\code{\link{snap_to_roads}()},
\code{\link{travel_cost_matrix}()}

Other vrp: 
\code{\link{last_mile_delivery}()},
\code{\link{solve_vrp_job}()}
}
\concept{direct}
\concept{vrp}
