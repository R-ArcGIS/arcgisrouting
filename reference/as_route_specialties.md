# Convert a data.frame to Last Mile Delivery route specialties input

Convert a data.frame to Last Mile Delivery route specialties input

## Usage

``` r
as_route_specialties(x, ...)
```

## Arguments

- x:

  A `data.frame` with columns `route_name` and `specialty_name`, or
  `NULL`.

- ...:

  Additional arguments passed to methods.
