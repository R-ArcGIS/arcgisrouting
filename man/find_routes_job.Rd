% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job-find-routes.R
\name{find_routes_job}
\alias{find_routes_job}
\title{Find Routes (Async)}
\usage{
find_routes_job(
  stops,
  travel_mode = NULL,
  measurement_units = NULL,
  analysis_region = NULL,
  reorder_stops = FALSE,
  preserve_terminal_stops = NULL,
  return_to_start = NULL,
  use_time_windows = NULL,
  time_of_day = NULL,
  uturn_at_junctions = NULL,
  use_hierarchy = NULL,
  restrictions = NULL,
  impedance = NULL,
  time_impedance = NULL,
  distance_impedance = NULL,
  route_shape = NULL,
  populate_route_edges = NULL,
  populate_directions = NULL,
  directions_language = NULL,
  directions_distance_units = NULL,
  directions_style_name = NULL,
  output_format = "feature_set",
  ignore_invalid_locations = TRUE,
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{stops}{An \code{sf} or \code{sfc} object containing point geometries.
Recognized attribute columns such as \code{route_name}, \code{time_window_start},
and \code{time_window_end} are used when present.}

\item{travel_mode}{Character. The name or ID of the travel mode to use.
See \code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{measurement_units}{Character. Units for reporting total travel time or
distance. One of: \code{"meters"}, \code{"kilometers"}, \code{"feet"}, \code{"yards"},
\code{"miles"}, \code{"nautical_miles"}, \code{"seconds"}, \code{"minutes"}, \code{"hours"},
\code{"days"}. Default: \code{NULL} (API default: \code{"minutes"}).}

\item{analysis_region}{Character. Region for the analysis. One of:
\code{"europe"}, \code{"japan"}, \code{"korea"}, \code{"middle_east_and_africa"},
\code{"north_america"}, \code{"south_america"}, \code{"south_asia"}, \code{"thailand"}.
Default: \code{NULL} (auto-detected).}

\item{reorder_stops}{Logical. Reorder stops to find the optimal route
(TSP). Default: \code{FALSE}.}

\item{preserve_terminal_stops}{Character. Which terminal stops to preserve
when \code{reorder_stops = TRUE}. One of: \code{"first"}, \code{"last"},
\code{"first_and_last"}, \code{"none"}. Default: \code{NULL} (API default: \code{"first"}).}

\item{return_to_start}{Logical. Whether the route should return to its
starting location. Default: \code{NULL} (API default: \code{TRUE}).}

\item{use_time_windows}{Logical. Whether to honour time windows on stops.
Default: \code{NULL} (auto-detected from \code{stops} attributes).}

\item{time_of_day}{POSIXct. The departure time for the routes. When \code{NULL},
static average speeds are used. Default: \code{NULL}.}

\item{uturn_at_junctions}{Character. U-turn policy at junctions. One of:
\code{"allow_backtrack"}, \code{"deadend_intersection"}, \code{"deadend"},
\code{"no_backtrack"}. Default: \code{NULL}.}

\item{use_hierarchy}{Logical. Whether to use the street hierarchy when
finding routes. Default: \code{NULL} (API default: \code{TRUE}).}

\item{restrictions}{Character vector. Restriction names to apply.
Default: \code{NULL}.}

\item{impedance}{Character. Impedance type. One of: \code{"travel_time"},
\code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"}, \code{"walk_time"},
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{time_impedance}{Character. Time-based impedance. One of:
\code{"minutes"}, \code{"travel_time"}, \code{"walk_time"}, \code{"truck_minutes"},
\code{"truck_travel_time"}. Default: \code{NULL}.}

\item{distance_impedance}{Character. Distance-based impedance. One of:
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{route_shape}{Character. Shape of the output route features. One of:
\code{"true_shape"}, \code{"true_shape_with_measures"}, \code{"straight_line"},
\code{"none"}. Default: \code{NULL} (API default: \code{"true_shape"}).}

\item{populate_route_edges}{Logical. Generate edges for each route.
Default: \code{NULL} (API default: \code{FALSE}).}

\item{populate_directions}{Logical. Generate driving directions.
Default: \code{NULL} (API default: \code{FALSE}).}

\item{directions_language}{Character. Language code for directions.
Default: \code{NULL} (API default: \code{"en"}).}

\item{directions_distance_units}{Character. Units for distances in
directions. One of: \code{"feet"}, \code{"kilometers"}, \code{"meters"}, \code{"miles"},
\code{"nautical_miles"}, \code{"yards"}. Default: \code{NULL}.}

\item{directions_style_name}{Character. Formatting style for directions.
One of: \code{"desktop"}, \code{"navigation"}. Default: \code{NULL}.}

\item{output_format}{Character. Format for output features. One of:
\code{"feature_set"}, \code{"json_file"}, \code{"geojson_file"}. Default:
\code{"feature_set"}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid
input locations. Default: \code{TRUE}.}

\item{point_barriers}{Point barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{line_barriers}{Line barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A \code{find_routes_job} R6 object inheriting from
\code{arcgisutils::arc_gp_job}. Call \verb{$start()} to submit and \verb{$results} to
retrieve output.
}
\description{
Submits an asynchronous geoprocessing job to find the best routes between
stops using the ArcGIS \verb{/FindRoutes} GP service.
}
\examples{
\dontrun{
# This example is not executed since it requires a network connection
# to ArcGIS Online and a valid authentication token
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

stops <- st_sf(
  name = c("Stop 1", "Stop 2", "Stop 3"),
  geometry = st_sfc(
    st_point(c(145.066, -37.865)),
    st_point(c(145.105, -37.819)),
    st_point(c(145.120, -37.800)),
    crs = 4326
  )
)

job <- find_routes_job(stops)
job$start()
result <- job$results
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/route-service-job/}{API Reference}
}
\seealso{
Other async: 
\code{\link{download_od_results}()},
\code{\link{download_service_area_results}()},
\code{\link{find_closest_facilities_job}()},
\code{\link{find_service_areas_job}()},
\code{\link{last_mile_delivery}()},
\code{\link{location_allocation_job}()},
\code{\link{od_cost_matrix_job}()},
\code{\link{route_vehicles_job}()}

Other routing: 
\code{\link{find_routes}()}
}
\concept{async}
\concept{routing}
