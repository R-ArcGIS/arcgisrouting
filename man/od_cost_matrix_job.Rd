% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job-od-cost-matrix.R
\name{od_cost_matrix_job}
\alias{od_cost_matrix_job}
\title{Generate Origin-Destination Cost Matrix (Async)}
\usage{
od_cost_matrix_job(
  origins,
  destinations = NULL,
  travel_mode = NULL,
  time_units = NULL,
  distance_units = NULL,
  analysis_region = NULL,
  n_dests = NULL,
  cutoff = NULL,
  time_of_day = NULL,
  uturn_at_junctions = NULL,
  use_hierarchy = NULL,
  restrictions = NULL,
  attribute_parameter_values = NULL,
  origin_destination_line_shape = NULL,
  impedance = NULL,
  time_impedance = NULL,
  distance_impedance = NULL,
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  save_output_network_analysis_layer = NULL,
  output_format = "feature_set",
  ignore_invalid_locations = TRUE,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{origins}{An \code{sf} or \code{sfc} object containing point geometries that
function as starting points in generating the paths to destinations.}

\item{destinations}{An \code{sf} or \code{sfc} object containing point geometries that
function as ending points in generating the paths from origins. Default:
\code{NULL}.}

\item{travel_mode}{Character. The name or ID of the travel mode to use.
See \code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{time_units}{Character. Units used to measure and report the total
travel time between each origin-destination pair. One of: \code{"seconds"},
\code{"minutes"}, \code{"hours"}, \code{"days"}. Default: \code{NULL} (API default:
\code{"minutes"}).}

\item{distance_units}{Character. Units used to measure and report the total
travel distance between each origin-destination pair. One of: \code{"meters"},
\code{"kilometers"}, \code{"feet"}, \code{"yards"}, \code{"miles"}, \code{"nautical_miles"}.
Default: \code{NULL} (API default: \code{"miles"}).}

\item{analysis_region}{Character. Region for the analysis. One of:
\code{"europe"}, \code{"japan"}, \code{"korea"}, \code{"middle_east_and_africa"},
\code{"north_america"}, \code{"south_america"}, \code{"south_asia"}, \code{"thailand"}.
Default: \code{NULL} (auto-detected).}

\item{n_dests}{Integer. The maximum number of destinations to find per
origin. Default: \code{NULL} (API default: every destination).}

\item{cutoff}{Numeric. The travel time or travel distance value at which to
stop searching for destinations from a given origin. Default: \code{NULL}.}

\item{time_of_day}{POSIXct. The departure time for the routes. When \code{NULL},
static average speeds are used. Default: \code{NULL}.}

\item{uturn_at_junctions}{Character. U-turn policy at junctions. One of:
\code{"allow_backtrack"}, \code{"deadend_intersection"}, \code{"deadend"},
\code{"no_backtrack"}. Default: \code{NULL}.}

\item{use_hierarchy}{Logical. Whether to use the street hierarchy when
finding routes. Default: \code{NULL} (API default: \code{TRUE}).}

\item{restrictions}{Character vector. Restriction names to apply.
Default: \code{NULL}.}

\item{attribute_parameter_values}{List. Additional values for attributes or restrictions, passed through as a JSON object. Default: \code{NULL}.}

\item{origin_destination_line_shape}{Character. Shape of the line feature
connecting each origin-destination pair in the output matrix. One of:
\code{"straight_line"} or \code{"none"}. Default: \code{NULL} (API default: \code{"none"}).}

\item{impedance}{Character. Impedance type. One of: \code{"travel_time"},
\code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"}, \code{"walk_time"},
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{time_impedance}{Character. Time-based impedance. One of:
\code{"minutes"}, \code{"travel_time"}, \code{"walk_time"}, \code{"truck_minutes"},
\code{"truck_travel_time"}. Default: \code{NULL}.}

\item{distance_impedance}{Character. Distance-based impedance. One of:
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{point_barriers}{Point barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{line_barriers}{Line barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{save_output_network_analysis_layer}{Logical. Whether to save the analysis as a network analysis layer package file. Default: \code{NULL} (API default: \code{FALSE}).}

\item{output_format}{Character. Format for output features. One of:
\code{"feature_set"}, \code{"json_file"}, \code{"geojson_file"}. Default:
\code{"feature_set"}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid
input locations. Default: \code{TRUE}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
An \code{od_cost_matrix_job} R6 object inheriting from
\code{arcgisutils::arc_gp_job}. Call \verb{$start()} to submit and \verb{$results} to
retrieve output.
}
\description{
Submits an asynchronous geoprocessing job that creates an origin-destination
(OD) cost matrix from multiple origins to multiple destinations using the
ArcGIS \verb{/GenerateOriginDestinationCostMatrix} GP service. The matrix reports
the travel time or travel distance from every origin to every destination.
}
\examples{
\dontrun{
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

origins <- st_sf(
  name = c("Origin 1", "Origin 2"),
  n_dests = c(2L, 3L),
  cutoff = c(120, 90),
  curb_approach = c(0L, 0L),
  geometry = st_sfc(
    st_point(c(-0.1891, 51.5254)),
    st_point(c(-0.1744, 51.5353)),
    crs = 4326
  )
)

destinations <- st_sf(
  name = c("Destination 1", "Destination 2"),
  curb_approach = c(0L, 0L),
  geometry = st_sfc(
    st_point(c(-0.1991, 51.5354)),
    st_point(c(-0.1844, 51.5458)),
    crs = 4326
  )
)

job <- od_cost_matrix_job(origins, destinations)
job$start()
result <- job$results
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/travelCostMatrix-service-job/}{API Reference}
}
\seealso{
Other async: 
\code{\link{download_od_results}()},
\code{\link{download_service_area_results}()},
\code{\link{find_closest_facilities_job}()},
\code{\link{find_routes_job}()},
\code{\link{find_service_areas_job}()},
\code{\link{last_mile_delivery}()},
\code{\link{location_allocation_job}()},
\code{\link{route_vehicles_job}()}

Other od: 
\code{\link{download_od_results}()},
\code{\link{od_cost_matrix}()}
}
\concept{async}
\concept{od}
