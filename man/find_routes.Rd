% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct-solve.R
\name{find_routes}
\alias{find_routes}
\title{Find Routes}
\usage{
find_routes(
  stops,
  travel_mode = NULL,
  start_time = NULL,
  find_best_sequence = FALSE,
  preserve_first_stop = TRUE,
  preserve_last_stop = TRUE,
  restrict_u_turns = NULL,
  use_hierarchy = NULL,
  impedance_attribute_name = NULL,
  accumulate_attribute_names = NULL,
  restrictions = NULL,
  barriers = NULL,
  polyline_barriers = NULL,
  polygon_barriers = NULL,
  return_directions = TRUE,
  directions_language = "en",
  return_routes = TRUE,
  return_stops = FALSE,
  ignore_invalid_locations = TRUE,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{stops}{An \code{sf} or \code{sfc} object containing point geometries representing
the stops to visit. Use \code{\link[=as_stops]{as_stops()}} to prepare stops with attributes.}

\item{travel_mode}{Character. The name of the travel mode to use. See
\code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{start_time}{POSIXct or character. The time at which travel begins.
Can be \code{"now"} for current time, a POSIXct datetime, or \code{NULL} for
static speeds. Default: \code{NULL}.}

\item{find_best_sequence}{Logical. Whether to reorder stops to find the
optimized route. Default: \code{FALSE}.}

\item{preserve_first_stop}{Logical. Whether to keep the first stop fixed
when reordering. Only applies if \code{find_best_sequence = TRUE}. Default: \code{TRUE}.}

\item{preserve_last_stop}{Logical. Whether to keep the last stop fixed
when reordering. Only applies if \code{find_best_sequence = TRUE}. Default: \code{TRUE}.}

\item{restrict_u_turns}{Character. Specifies U-turn restrictions. One of:
\code{"allow_backtrack"}, \code{"deadend_intersection"}, \code{"deadend"}, \code{"no_backtrack"}.
Default: \code{"allow_backtrack"}.}

\item{use_hierarchy}{Logical. Whether to use hierarchy when finding routes.
Default: \code{TRUE}.}

\item{impedance_attribute_name}{Character. The impedance to use. One of:
\code{"travel_time"}, \code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"},
\code{"walk_time"}, \code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{accumulate_attribute_names}{Character vector. Additional impedance
values to accumulate. Default: \code{NULL}.}

\item{restrictions}{Character vector. Restriction names to honor.
Default: \code{NULL}.}

\item{barriers}{Point barriers as \code{sf} or \code{sfc} object. Default: \code{NULL}.}

\item{polyline_barriers}{Line barriers as \code{sf} or \code{sfc} object. Default: \code{NULL}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object. Default: \code{NULL}.}

\item{return_directions}{Logical. Whether to generate driving directions.
Default: \code{TRUE}.}

\item{directions_language}{Character. Language code for directions (e.g., \code{"en"}).
Default: \code{"en"}.}

\item{return_routes}{Logical. Whether to return route geometries. Default: \code{TRUE}.}

\item{return_stops}{Logical. Whether to return stops in output. Default: \code{FALSE}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid locations.
Default: \code{TRUE}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A list containing the routing results.
}
\description{
Finds the best routes between multiple stops using the ArcGIS routing service.
}
\examples{
\dontrun{
library(sf)

# Simple route between 3 stops
stops <- st_sf(
  name = c("Start", "Middle", "End"),
  geometry = st_sfc(
    st_point(c(-122.4194, 37.7749)),  # San Francisco
    st_point(c(-122.0312, 37.3318)),  # Cupertino
    st_point(c(-121.8863, 37.3382)),  # San Jose
    crs = 4326
  )
)

result <- find_routes(stops)

# Route with time windows for deliveries
delivery_stops <- st_sf(
  name = c("Warehouse", "Customer A", "Customer B", "Customer C"),
  time_window_start = as.POSIXct(c(
    "2024-01-15 08:00:00",
    "2024-01-15 09:00:00",
    "2024-01-15 11:00:00",
    "2024-01-15 14:00:00"
  )),
  time_window_end = as.POSIXct(c(
    "2024-01-15 08:30:00",
    "2024-01-15 10:00:00",
    "2024-01-15 13:00:00",
    "2024-01-15 16:00:00"
  )),
  geometry = st_sfc(
    st_point(c(-122.4194, 37.7749)),
    st_point(c(-122.4083, 37.7858)),
    st_point(c(-122.4313, 37.7793)),
    st_point(c(-122.4000, 37.7900)),
    crs = 4326
  )
)

delivery_route <- find_routes(
  delivery_stops,
  start_time = as.POSIXct("2024-01-15 08:00:00"),
  travel_mode = "Driving Time"
)

# Find optimal order for stops
sales_stops <- st_sf(
  name = c("Office", "Client 1", "Client 2", "Client 3", "Office"),
  geometry = st_sfc(
    st_point(c(-122.4194, 37.7749)),
    st_point(c(-122.4083, 37.7858)),
    st_point(c(-122.4313, 37.7793)),
    st_point(c(-122.4000, 37.7900)),
    st_point(c(-122.4194, 37.7749)),
    crs = 4326
  )
)

optimized_route <- find_routes(
  sales_stops,
  find_best_sequence = TRUE,
  preserve_first_stop = TRUE,
  preserve_last_stop = TRUE
)

# Route with barriers
barrier_pts <- st_sfc(
  st_point(c(-122.4150, 37.7800)),
  crs = 4326
)

route_with_barriers <- find_routes(
  stops,
  barriers = barrier_pts
)

# Accumulate multiple impedances
multi_impedance_route <- find_routes(
  stops,
  impedance_attribute_name = "travel_time",
  accumulate_attribute_names = c("miles", "kilometers")
)
}

}
