% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job-solve-vrp.R
\name{solve_vrp_job}
\alias{solve_vrp_job}
\title{Solve Vehicle Routing Problem (Async)}
\usage{
solve_vrp_job(
  orders,
  depots,
  routes = NULL,
  breaks = NULL,
  travel_mode = NULL,
  analysis_region = NULL,
  time_zone_usage_for_time_fields = NULL,
  default_date = NULL,
  time_units = NULL,
  distance_units = NULL,
  time_window_factor = NULL,
  spatially_cluster_routes = TRUE,
  route_zones = NULL,
  route_renewals = NULL,
  order_pairs = NULL,
  excess_transit_factor = NULL,
  uturn_policy = NULL,
  restrictions = NULL,
  impedance = NULL,
  time_impedance = NULL,
  distance_impedance = NULL,
  use_hierarchy_in_analysis = TRUE,
  populate_route_lines = TRUE,
  route_line_simplification_tolerance = NULL,
  populate_directions = FALSE,
  directions_language = NULL,
  directions_style_name = NULL,
  save_route_data = FALSE,
  save_output_layer = FALSE,
  populate_stop_shapes = FALSE,
  output_format = "feature_set",
  ignore_invalid_order_locations = FALSE,
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{orders}{An \code{sf} or \code{sfc} object containing point geometries
representing locations to visit.}

\item{depots}{An \code{sf} or \code{sfc} object containing point geometries
representing depot locations.}

\item{routes}{A \code{data.frame} describing vehicle and driver characteristics.
Default: \code{NULL}.}

\item{breaks}{A \code{data.frame} of rest period definitions for routes.
Default: \code{NULL}.}

\item{travel_mode}{Character. The name or ID of the travel mode to use.
See \code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{analysis_region}{Character. Region for the analysis. One of:
\code{"europe"}, \code{"japan"}, \code{"korea"}, \code{"middle_east_and_africa"},
\code{"north_america"}, \code{"south_america"}, \code{"south_asia"}, \code{"thailand"}.
Default: \code{NULL} (auto-detected).}

\item{time_zone_usage_for_time_fields}{Character. Time zone used for all
date-time input fields. One of: \code{"geo_local"}, \code{"utc"}. Default: \code{NULL}
(API default: \code{"geo_local"}).}

\item{default_date}{POSIXct. The date on which all routes start. Only the
date portion is used. Default: \code{NULL}.}

\item{time_units}{Character. Units for all time-based attribute values. One
of: \code{"seconds"}, \code{"minutes"}, \code{"hours"}, \code{"days"}. Default: \code{NULL}
(API default: \code{"minutes"}).}

\item{distance_units}{Character. Units for all distance-based attribute
values. One of: \code{"miles"}, \code{"kilometers"}, \code{"meters"}, \code{"feet"},
\code{"yards"}, \code{"nautical_miles"}. Default: \code{NULL} (API default: \code{"miles"}).}

\item{time_window_factor}{Character. Importance of honoring time windows.
One of: \code{"low"}, \code{"medium"}, \code{"high"}. Default: \code{NULL}
(API default: \code{"medium"}).}

\item{spatially_cluster_routes}{Logical. Whether orders assigned to a route
are spatially clustered. Default: \code{TRUE}.}

\item{route_zones}{An \code{sf} or \code{sfc} polygon object delineating work
territories for routes. Default: \code{NULL}.}

\item{route_renewals}{A \code{data.frame} of intermediate depot renewal locations.
Default: \code{NULL}.}

\item{order_pairs}{A \code{data.frame} pairing pickup and delivery orders.
Default: \code{NULL}.}

\item{excess_transit_factor}{Character. Importance of reducing excess transit
time for order pairs. One of: \code{"low"}, \code{"medium"}, \code{"high"}. Default:
\code{NULL} (API default: \code{"medium"}).}

\item{uturn_policy}{Character. U-turn policy at junctions. One of:
\code{"allow_uturn"}, \code{"allow_dead_ends_and_intersections_only"},
\code{"allow_dead_ends_only"}, \code{"no_uturns"}. Default: \code{NULL}.}

\item{restrictions}{Character vector. Restriction names to apply.
Default: \code{NULL}.}

\item{impedance}{Character. Impedance type. One of: \code{"travel_time"},
\code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"}, \code{"walk_time"},
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{time_impedance}{Character. Time-based impedance. One of:
\code{"minutes"}, \code{"travel_time"}, \code{"walk_time"}, \code{"truck_minutes"},
\code{"truck_travel_time"}. Default: \code{NULL}.}

\item{distance_impedance}{Character. Distance-based impedance. One of:
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{use_hierarchy_in_analysis}{Logical. Whether to use the street network
hierarchy when finding routes. Default: \code{TRUE}.}

\item{populate_route_lines}{Logical. Whether output routes include the exact
street shape. Default: \code{TRUE}.}

\item{route_line_simplification_tolerance}{Numeric. Simplification tolerance
for route geometry. Default: \code{NULL}.}

\item{populate_directions}{Logical. Whether to generate driving directions
for each route. Default: \code{FALSE}.}

\item{directions_language}{Character. Language code for directions (e.g.
\code{"en"}). Default: \code{NULL}.}

\item{directions_style_name}{Character. Formatting style for directions. One
of: \code{"desktop"}, \code{"navigation"}, \code{"campus"}. Default: \code{NULL}.}

\item{save_route_data}{Logical. Whether to save results as a \code{.zip} file
geodatabase. Default: \code{FALSE}.}

\item{save_output_layer}{Logical. Whether to save the analysis as a network
analysis layer package. Default: \code{FALSE}.}

\item{populate_stop_shapes}{Logical. Whether output stops include point
geometries. Default: \code{FALSE}.}

\item{output_format}{Character. Format for output features. One of:
\code{"feature_set"}, \code{"json_file"}, \code{"geojson_file"}. Default:
\code{"feature_set"}.}

\item{ignore_invalid_order_locations}{Logical. Whether to ignore invalid
orders instead of failing. Default: \code{FALSE}.}

\item{point_barriers}{Point barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{line_barriers}{Line barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A \code{solve_vrp_job} R6 object inheriting from
\code{arcgisutils::arc_gp_job}. Call \verb{$start()} to submit and \verb{$results} to
retrieve output.
}
\description{
Submits an asynchronous geoprocessing job to solve a vehicle routing problem
using the ArcGIS \verb{/SolveVehicleRoutingProblem} GP service.
}
\examples{
\dontrun{
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

orders <- st_sf(
  name = c("Order 1", "Order 2"),
  geometry = st_sfc(
    st_point(c(-0.1891, 51.5254)),
    st_point(c(-0.1744, 51.5353)),
    crs = 4326
  )
)

depots <- st_sf(
  name = "Depot1",
  geometry = st_sfc(st_point(c(-0.2, 51.5)), crs = 4326)
)

job <- solve_vrp_job(orders, depots)
job$start()
result <- job$results
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/vrp-service-job/}{API Reference}
}
\seealso{
Other async: 
\code{\link{download_od_results}()},
\code{\link{download_service_area_results}()},
\code{\link{find_closest_facilities_job}()},
\code{\link{find_routes_job}()},
\code{\link{last_mile_delivery}()},
\code{\link{location_allocation_job}()},
\code{\link{od_cost_matrix_job}()},
\code{\link{solve_service_areas_job}()}

Other vrp: 
\code{\link{last_mile_delivery}()},
\code{\link{route_vehicles}()}
}
\concept{async}
\concept{vrp}
