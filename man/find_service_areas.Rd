% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct-solve-service-area.R
\name{find_service_areas}
\alias{find_service_areas}
\title{Solve Service Area}
\usage{
find_service_areas(
  facilities,
  default_breaks = c(5, 10, 15),
  travel_mode = NULL,
  travel_direction = "away",
  time_of_day = NULL,
  output_polygons = "simplified",
  split_polygons_at_breaks = TRUE,
  overlap_polygons = TRUE,
  merge_similar_polygon_ranges = FALSE,
  trim_outer_polygons = TRUE,
  output_lines = NULL,
  split_lines_at_breaks = TRUE,
  overlap_lines = TRUE,
  return_geometry = "service_areas",
  u_turns = NULL,
  use_hierarchy = NULL,
  impedance = NULL,
  accumulate_impedance = NULL,
  restrictions = NULL,
  trim_polygon_distance = 100,
  trim_polygon_distance_units = "meters",
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  ignore_invalid_locations = TRUE,
  output_geometry_precision = 10,
  output_geometry_precision_units = "meters",
  crs = 4326,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{facilities}{an \code{sf} or \code{sfc} object containing point geometries representing the facilities around which service areas are generated.}

\item{default_breaks}{default \code{c(5, 10, 15)}. A numeric vector specifying the size and number of service areas to generate for each facility. Units are determined by the \code{impedance} parameter.}

\item{travel_mode}{Character. The name of the travel mode to use. See
\code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{travel_direction}{default \code{"away"}. A scalar character. One of \code{"away"} (away from facility) or \code{"towards"} (toward facility).}

\item{output_polygons}{default \code{"simplified"}. A scalar character or \code{NULL} (no polygons). One of \code{"simplified"} or \code{"detailed"}.}

\item{split_polygons_at_breaks}{default \code{TRUE}. A logical scalar. When \code{TRUE}, service areas appear as rings between breaks. When \code{FALSE}, each area is a disk from the facility to the break.}

\item{overlap_polygons}{default \code{TRUE}. A logical scalar. Whether service area polygons from different facilities can overlap.}

\item{merge_similar_polygon_ranges}{default \code{FALSE}. A logical scalar. Whether service area polygons from different facilities with the same break value are merged into a single polygon.}

\item{trim_outer_polygons}{default \code{TRUE}. A logical scalar. Whether service areas are trimmed to lie within a distance of the network. Ignored when \code{use_hierarchy = TRUE}.}

\item{output_lines}{default \code{NULL} (no lines). A scalar character or \code{NULL}. One of \code{"true_shape"} or \code{"with_measure"}.}

\item{split_lines_at_breaks}{default \code{TRUE}. A logical scalar. Whether service area lines are split at break values.}

\item{overlap_lines}{default \code{TRUE}. A logical scalar. Whether service area lines from different facilities can overlap.}

\item{return_geometry}{default \code{"service_areas"}. A character vector. Valid values: \code{"service_areas"}, \code{"sa_lines"}, \code{"facilities"}, \code{"barriers"}, \code{"polyline_barriers"}, \code{"polygon_barriers"}.}

\item{use_hierarchy}{Logical. Whether to use hierarchy when finding routes.
Default: \code{TRUE}.}

\item{restrictions}{Character vector. Restriction names to honor.
Default: \code{NULL}.}

\item{trim_polygon_distance}{default \code{100}. An integer scalar. The distance within which the service area polygon extends from the network.}

\item{trim_polygon_distance_units}{default \code{"meters"}. A scalar character. Units for \code{trim_polygon_distance}. One of \code{"meters"}, \code{"kilometers"}, \code{"feet"}, \code{"miles"}, \code{"nautical_miles"}, \code{"yards"}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object. Default: \code{NULL}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid locations.
Default: \code{TRUE}.}

\item{output_geometry_precision}{default \code{10}. A numeric scalar. Simplification tolerance applied to output geometry.}

\item{output_geometry_precision_units}{default \code{"meters"}. A scalar character. Units for \code{output_geometry_precision}. Same valid values as \code{trim_polygon_distance_units}.}

\item{crs}{default \code{4326}. The coordinate reference system of the output geometries. Passed to \code{arcgisutils::as_spatial_reference()}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A list containing the service area results.
}
\description{
Solve Service Area
}
\examples{
\dontrun{
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

facility <- st_sfc(st_point(c(-122.253, 37.757)), crs = 4326)
find_service_areas(facility)
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/service-area-service-direct/}{API Reference}
}
\seealso{
Other direct: 
\code{\link{find_closest_facility}()},
\code{\link{find_routes}()},
\code{\link{route_vehicles}()},
\code{\link{snap_to_roads}()},
\code{\link{travel_cost_matrix}()}

Other service area: 
\code{\link{download_service_area_results}()},
\code{\link{solve_service_areas_job}()}
}
\concept{direct}
\concept{service area}
