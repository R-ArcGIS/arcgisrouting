% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job-location-allocation.R
\name{location_allocation_job}
\alias{location_allocation_job}
\title{Solve Location-Allocation (Async)}
\usage{
location_allocation_job(
  facilities,
  demand_points,
  travel_mode = NULL,
  locate_settings = NULL,
  measurement_units = NULL,
  analysis_region = NULL,
  problem_type = NULL,
  number_of_facilities_to_find = NULL,
  default_measurement_cutoff = NULL,
  default_capacity = NULL,
  target_market_share = NULL,
  measurement_transformation_model = NULL,
  measurement_transformation_factor = NULL,
  travel_direction = NULL,
  time_of_day = NULL,
  uturn_at_junctions = NULL,
  use_hierarchy = NULL,
  restrictions = NULL,
  attribute_parameter_values = NULL,
  allocation_line_shape = NULL,
  time_impedance = NULL,
  distance_impedance = NULL,
  impedance = NULL,
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  save_output_network_analysis_layer = NULL,
  output_format = "feature_set",
  ignore_invalid_locations = TRUE,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{facilities}{An \code{sf} or \code{sfc} object containing point geometries
representing the locations that serve as facilities.}

\item{demand_points}{An \code{sf} or \code{sfc} object containing point geometries
representing the demand points.}

\item{travel_mode}{Character. The name or ID of the travel mode to use.
See \code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{locate_settings}{List. Settings that affect how input data are located,
passed through as a JSON object. Default: \code{NULL}.}

\item{measurement_units}{Character. Units for reporting total travel time or
distance. One of: \code{"meters"}, \code{"kilometers"}, \code{"feet"}, \code{"yards"},
\code{"miles"}, \code{"nautical_miles"}, \code{"seconds"}, \code{"minutes"}, \code{"hours"},
\code{"days"}. Default: \code{NULL} (API default: \code{"minutes"}).}

\item{analysis_region}{Character. Region for the analysis. One of:
\code{"europe"}, \code{"japan"}, \code{"korea"}, \code{"middle_east_and_africa"},
\code{"north_america"}, \code{"south_america"}, \code{"south_asia"}, \code{"thailand"}.
Default: \code{NULL} (auto-detected).}

\item{problem_type}{Character. Objective of the location-allocation analysis.
One of: \code{"maximize_attendance"}, \code{"maximize_capacitated_coverage"},
\code{"maximize_coverage"}, \code{"maximize_market_share"}, \code{"minimize_facilities"},
\code{"minimize_impedance"}, \code{"target_market_share"}. Default: \code{NULL}
(API default: \code{"minimize_impedance"}).}

\item{number_of_facilities_to_find}{Integer. The number of facilities the
task should choose. Default: \code{NULL} (API default: \code{1}).}

\item{default_measurement_cutoff}{Numeric. The maximum travel time or distance
allowed between a demand point and the facility to which it is allocated.
Units are determined by \code{measurement_units}. Default: \code{NULL} (API default:
no cutoff).}

\item{default_capacity}{Numeric. The default capacity assigned to all
facilities. Only applicable to the \code{"maximize_capacitated_coverage"}
problem type. Default: \code{NULL} (API default: \code{1}).}

\item{target_market_share}{Numeric. The percentage of total demand weight to
capture. Only applicable to the \code{"target_market_share"} problem type.
Default: \code{NULL} (API default: \code{10}).}

\item{measurement_transformation_model}{Character. Equation for transforming
the network cost between facilities and demand points. One of: \code{"linear"},
\code{"power"}, \code{"exponential"}. Default: \code{NULL} (API default: \code{"linear"}).}

\item{measurement_transformation_factor}{Numeric. The impedance parameter
value (lambda) for \code{measurement_transformation_model}. Ignored when the
model is linear. Default: \code{NULL} (API default: \code{1}).}

\item{travel_direction}{Character. Direction in which to measure travel. One
of: \code{"facility_to_demand"} or \code{"demand_to_facility"}. Default: \code{NULL}
(API default: \code{"facility_to_demand"}).}

\item{time_of_day}{POSIXct. The departure time for the routes. When \code{NULL},
static average speeds are used. Default: \code{NULL}.}

\item{uturn_at_junctions}{Character. U-turn policy at junctions. One of:
\code{"allow_backtrack"}, \code{"deadend_intersection"}, \code{"deadend"},
\code{"no_backtrack"}. Default: \code{NULL}.}

\item{use_hierarchy}{Logical. Whether to use the street hierarchy when
finding routes. Default: \code{NULL} (API default: \code{TRUE}).}

\item{restrictions}{Character vector. Restriction names to apply.
Default: \code{NULL}.}

\item{allocation_line_shape}{Character. Type of line features output by the
request. One of: \code{"straight_line"} or \code{"none"}. Default: \code{NULL}
(API default: \code{"straight_line"}).}

\item{time_impedance}{Character. Time-based impedance. One of:
\code{"minutes"}, \code{"travel_time"}, \code{"walk_time"}, \code{"truck_minutes"},
\code{"truck_travel_time"}. Default: \code{NULL}.}

\item{distance_impedance}{Character. Distance-based impedance. One of:
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{impedance}{Character. Impedance type. One of: \code{"travel_time"},
\code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"}, \code{"walk_time"},
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{point_barriers}{Point barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{line_barriers}{Line barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{output_format}{Character. Format for output features. One of:
\code{"feature_set"}, \code{"json_file"}, \code{"geojson_file"}. Default:
\code{"feature_set"}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid
input locations. Default: \code{TRUE}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A \code{location_allocation_job} R6 object inheriting from
\code{arcgisutils::arc_gp_job}. Call \verb{$start()} to submit and \verb{$results} to
retrieve output.
}
\description{
Submits an asynchronous geoprocessing job that chooses the set of facilities
which best serve demand from surrounding areas, simultaneously locating
facilities and allocating demand points to them, using the ArcGIS
\verb{/SolveLocationAllocation} GP service.
}
\examples{
\dontrun{
library(sf)
set_arc_token(auth_user())
facilities <- st_sf(
  name = c("Facility A", "Facility B"),
  facility_type = c(0L, 0L),
  curb_approach = c(0L, 0L),
  geometry = st_sfc(
    st_point(c(-58.557329417999938, -34.587693706999971)),
    st_point(c(-58.460247408999976, -34.683348039999942)),
    crs = 4326
  )
)

demand_points <- st_sf(
  name = c("Household 4", "Household 3", "Household 2", "Household 1"),
  group_name = c("A", "A", NA, NA),
  weight = c(2, 2, 3, 5),
  curb_approach = c(0L, 0L, 0L, 1L),
  geometry = st_sfc(
    st_point(c(-58.664405163999959, -34.614819562999969)),
    st_point(c(-58.514499119999982, -34.496322404999944)),
    st_point(c(-58.54162497599998, -34.788996107999935)),
    st_point(c(-58.40599569799997, -34.637662387999967)),
    crs = 4326
  )
)

job <- location_allocation_job(facilities, demand_points)
job$start()
result <- job$results
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/solve-location-allocation/}{API Reference}
}
