% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job-solve-service-areas.R
\name{solve_service_areas_job}
\alias{solve_service_areas_job}
\title{Generate Service Areas (Async)}
\usage{
solve_service_areas_job(
  facilities,
  break_values = NULL,
  break_units = NULL,
  travel_mode = NULL,
  travel_direction = NULL,
  time_of_day = NULL,
  use_hierarchy = NULL,
  uturn_at_junctions = NULL,
  polygons_for_multiple_facilities = NULL,
  polygon_overlap_type = NULL,
  detailed_polygons = NULL,
  polygon_trim_distance = NULL,
  polygon_simplification_tolerance = NULL,
  polygon_detail = NULL,
  output_type = NULL,
  analysis_region = NULL,
  restrictions = NULL,
  impedance = NULL,
  time_impedance = NULL,
  distance_impedance = NULL,
  ignore_invalid_locations = TRUE,
  output_format = "feature_set",
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{facilities}{An \code{sf} or \code{sfc} object containing point geometries
representing the facilities around which service areas are generated.}

\item{break_values}{Numeric vector. Service area sizes. Units are determined
by \code{break_units}. Default: \code{NULL} (API default: \verb{5 10 15}).}

\item{break_units}{Character. Units for \code{break_values}. One of:
\code{"meters"}, \code{"kilometers"}, \code{"feet"}, \code{"yards"}, \code{"miles"},
\code{"nautical_miles"}, \code{"seconds"}, \code{"minutes"}, \code{"hours"}, \code{"days"}.
Default: \code{NULL} (API default: \code{"minutes"}).}

\item{travel_mode}{Character. The name or ID of the travel mode to use.
See \code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{travel_direction}{Character. Direction of travel relative to
facilities. One of: \code{"away"}, \code{"towards"}. Default: \code{NULL}
(API default: \code{"away"}).}

\item{time_of_day}{POSIXct. The departure time for the routes. When \code{NULL},
static average speeds are used. Default: \code{NULL}.}

\item{use_hierarchy}{Logical. Whether to use the street hierarchy when
finding routes. Default: \code{NULL} (API default: \code{TRUE}).}

\item{uturn_at_junctions}{Character. U-turn policy at junctions. One of:
\code{"allow_backtrack"}, \code{"deadend_intersection"}, \code{"deadend"},
\code{"no_backtrack"}. Default: \code{NULL}.}

\item{polygons_for_multiple_facilities}{Character. How service area polygons
from multiple facilities are generated. One of: \code{"overlapping"},
\code{"not_overlapping"}, \code{"merge"}. Default: \code{NULL}
(API default: \code{"overlapping"}).}

\item{polygon_overlap_type}{Character. Whether polygons are rings or disks.
One of: \code{"rings"}, \code{"disks"}. Default: \code{NULL} (API default: \code{"rings"}).}

\item{detailed_polygons}{Logical. Generate detailed polygons. Default: \code{NULL}
(API default: \code{FALSE}).}

\item{polygon_trim_distance}{List with elements \code{distance} (numeric) and
\code{units} (character). Trim polygons to within this distance of the network.
Default: \code{NULL}.}

\item{polygon_simplification_tolerance}{List with elements \code{distance}
(numeric) and \code{units} (character). Simplification tolerance for output
polygons. Default: \code{NULL}.}

\item{polygon_detail}{Character. Level of detail for output polygons. One
of: \code{"standard"}, \code{"generalized"}, \code{"high"}. Default: \code{NULL}
(API default: \code{"standard"}).}

\item{output_type}{Character. Type of output to generate. One of:
\code{"polygons"}, \code{"lines"}, \code{"polygons_and_lines"}. Default: \code{NULL}
(API default: \code{"polygons"}).}

\item{analysis_region}{Character. Region for the analysis. One of:
\code{"europe"}, \code{"japan"}, \code{"korea"}, \code{"middle_east_and_africa"},
\code{"north_america"}, \code{"south_america"}, \code{"south_asia"}, \code{"thailand"}.
Default: \code{NULL} (auto-detected).}

\item{restrictions}{Character vector. Restriction names to apply.
Default: \code{NULL}.}

\item{impedance}{Character. Impedance type. One of: \code{"travel_time"},
\code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"}, \code{"walk_time"},
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{time_impedance}{Character. Time-based impedance. One of:
\code{"minutes"}, \code{"travel_time"}, \code{"walk_time"}, \code{"truck_minutes"},
\code{"truck_travel_time"}. Default: \code{NULL}.}

\item{distance_impedance}{Character. Distance-based impedance. One of:
\code{"miles"}, \code{"kilometers"}. Default: \code{NULL}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid input
locations. Default: \code{TRUE}.}

\item{output_format}{Character. Format for output features. One of:
\code{"feature_set"}, \code{"json_file"}, \code{"geojson_file"}. Default:
\code{"feature_set"}.}

\item{point_barriers}{Point barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{line_barriers}{Line barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A \code{solve_service_areas_job} R6 object inheriting from
\code{arcgisutils::arc_gp_job}. Call \verb{$start()} to submit and \verb{$results} to
retrieve output.
}
\description{
Submits an asynchronous geoprocessing job to generate service areas around
facilities using the ArcGIS \verb{/GenerateServiceAreas} GP service.
}
\examples{
\dontrun{
library(sf)

facilities <- st_sfc(
  st_point(c(-122.4194, 37.7749)),
  st_point(c(-122.0312, 37.3318)),
  crs = 4326
)

job <- solve_service_areas_job(facilities, break_values = c(5, 10, 15))
job$start()
result <- job$results
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/serviceArea-service-job/}{API Reference}
}
