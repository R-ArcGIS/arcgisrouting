# Convert a data.frame to Last Mile Delivery routes input

Convert a data.frame to Last Mile Delivery routes input

## Usage

``` r
as_lmd_routes(x, ...)
```

## Arguments

- x:

  A `data.frame` describing vehicle and driver characteristics, or
  `NULL`.

- ...:

  Additional arguments passed to methods.
