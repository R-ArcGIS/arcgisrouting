% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct-solve-closest-facility.R
\name{find_closest_facilities}
\alias{find_closest_facilities}
\title{Find Closest Facilities}
\usage{
find_closest_facilities(
  incidents,
  facilities,
  travel_mode = NULL,
  default_target_facility_count = NULL,
  travel_direction = NULL,
  default_cutoff = NULL,
  time_of_day = NULL,
  time_of_day_usage = NULL,
  u_turns = NULL,
  use_hierarchy = NULL,
  impedance = NULL,
  accumulate_impedance = NULL,
  restrictions = NULL,
  attribute_parameter_values = NULL,
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  return_geometry = c("facilities", "incidents"),
  directions_language = NULL,
  directions_output_type = NULL,
  directions_style = NULL,
  directions_length_units = NULL,
  directions_time_attribute = NULL,
  output_lines = "true_shape",
  ignore_invalid_locations = TRUE,
  preserve_object_id = FALSE,
  return_empty_results = FALSE,
  output_geometry_precision = 10,
  output_geometry_precision_units = "meters",
  geometry_precision = NULL,
  geometry_precision_m = NULL,
  locate_settings = NULL,
  crs = 4326,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{incidents}{an \code{sf} or \code{sfc} object containing point geometries representing the locations to search from.}

\item{facilities}{an \code{sf} or \code{sfc} object containing point geometries representing the facilities to search for.}

\item{travel_mode}{Character. The name of the travel mode to use. See
\code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{default_target_facility_count}{default \code{NULL}. An integer scalar. The number of closest facilities to find per incident.}

\item{travel_direction}{default \code{"away"}. A scalar character. One of \code{"away"} (away from facility) or \code{"towards"} (toward facility).}

\item{default_cutoff}{default \code{NULL}. A numeric scalar. The travel time or distance value at which to stop searching for facilities.}

\item{time_of_day_usage}{default \code{NULL}. A scalar character. One of \code{"start_time"} or \code{"end_time"}. Specifies whether \code{time_of_day} represents departure or arrival time.}

\item{use_hierarchy}{Logical. Whether to use hierarchy when finding routes.
Default: \code{NULL}.}

\item{restrictions}{Character vector. Restriction names to honor.
Default: \code{NULL}.}

\item{attribute_parameter_values}{default \code{NULL}. A list of objects. Additional values required by an attribute or restriction.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object. Default: \code{NULL}.}

\item{return_geometry}{default \code{c("facilities", "incidents")}. A character vector. Valid values: \code{"cf_routes"}, \code{"facilities"}, \code{"incidents"}, \code{"directions"}, \code{"barriers"}, \code{"polyline_barriers"}, \code{"polygon_barriers"}, \code{"traversed_edges"}, \code{"traversed_junctions"}, \code{"traversed_turns"}. Use \code{"everything"} to return all.}

\item{directions_language}{Character. Language code for directions (e.g., \code{"en"}).
Default: \code{"en"}.}

\item{directions_output_type}{default \code{NULL}. A scalar character. One of \code{"standard"}, \code{"complete"}, \code{"complete_no_events"}, \code{"instructions_only"}, \code{"summary_only"}, \code{"feature_sets"}.}

\item{directions_style}{default \code{NULL}. A scalar character. One of \code{"desktop"}, \code{"navigation"}, \code{"campus"}.}

\item{directions_length_units}{default \code{NULL}. A scalar character. One of \code{"miles"}, \code{"kilometers"}, \code{"feet"}, \code{"meters"}, \code{"yards"}, \code{"nautical_miles"}.}

\item{directions_time_attribute}{default \code{NULL}. A scalar character. The time-based impedance attribute used for direction durations.}

\item{output_lines}{default \code{NULL} (no lines). A scalar character or \code{NULL}. One of \code{"true_shape"} or \code{"with_measure"}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid locations.
Default: \code{TRUE}.}

\item{preserve_object_id}{default \code{FALSE}. A logical scalar. Preserves object IDs from input locations in the output.}

\item{return_empty_results}{default \code{FALSE}. A logical scalar. Returns empty results instead of an error on failure.}

\item{output_geometry_precision}{default \code{10}. A numeric scalar. Simplification tolerance applied to output geometry.}

\item{output_geometry_precision_units}{default \code{"meters"}. A scalar character. Units for \code{output_geometry_precision}. Same valid values as \code{trim_polygon_distance_units}.}

\item{geometry_precision}{default \code{NULL}. An integer scalar. Decimal places for x and y values in response geometries.}

\item{geometry_precision_m}{default \code{NULL}. A scalar character. Decimal places for m-values in response geometries.}

\item{locate_settings}{default \code{NULL}. A list controlling how inputs are located on the network.}

\item{crs}{default \code{4326}. The coordinate reference system of the output geometries. Passed to \code{arcgisutils::as_spatial_reference()}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A named list. Elements present depend on \code{return_geometry}:
\itemize{
\item \code{cf_routes}: route features between incidents and facilities
\item \code{facilities}: facility features
\item \code{incidents}: incident features
\item \code{direction_points}: point features for direction maneuvers
\item \code{direction_lines}: line features for route segments
\item \code{barriers}: point barrier features
\item \code{polyline_barriers}: polyline barrier features
\item \code{polygon_barriers}: polygon barrier features
\item \code{traversed_edges}: traversed edge features
\item \code{traversed_junctions}: traversed junction features
\item \code{traversed_turns}: traversed turn features
\item \code{messages}: status and warning messages from the service
}
}
\description{
Finds one or more nearby facilities from incidents based on travel time or distance.
}
\examples{
\dontrun{
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

incidents <- st_sfc(st_point(c(-122.4496, 37.7467)), crs = 4326)

facilities <- st_sf(
  name = c("Station 11", "Station 20", "Station 24", "Station 39"),
  geometry = st_sfc(
    st_point(c(-122.4267, 37.7486)),
    st_point(c(-122.4561, 37.7513)),
    st_point(c(-122.4409, 37.7533)),
    st_point(c(-122.4578, 37.7407)),
    crs = 4326
  )
)

result <- find_closest_facilities(
  incidents = incidents,
  facilities = facilities,
  default_target_facility_count = 2,
  travel_direction = "away",
  default_cutoff = 3,
  return_geometry = "cf_routes",
  directions_length_units = "miles",
  crs = 3857
)

result
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/closestFacility-service-direct/}{API Reference}
}
\seealso{
Other direct: 
\code{\link{find_routes}()},
\code{\link{find_service_areas}()},
\code{\link{od_cost_matrix}()},
\code{\link{route_vehicles}()},
\code{\link{snap_to_roads}()}

Other closest facility: 
\code{\link{find_closest_facilities_job}()}
}
\concept{closest facility}
\concept{direct}
