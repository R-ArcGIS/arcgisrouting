% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direct-travel-cost-matrix.R
\name{od_cost_matrix}
\alias{od_cost_matrix}
\title{Origin-Destination Cost Matrix}
\usage{
od_cost_matrix(
  origins,
  destinations = origins,
  travel_mode = NULL,
  default_cutoff = NULL,
  default_target_destination_count = NULL,
  output_type = NULL,
  time_of_day = NULL,
  u_turns = NULL,
  use_hierarchy = NULL,
  impedance = NULL,
  accumulate_impedance = NULL,
  restrictions = NULL,
  attribute_parameter_values = NULL,
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  return_geometry = character(0),
  ignore_invalid_locations = TRUE,
  return_empty_results = FALSE,
  geometry_precision = NULL,
  locate_settings = NULL,
  crs = 4326,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{origins}{an \code{sf} or \code{sfc} object containing point geometries representing the starting points.}

\item{destinations}{default \code{origins}. An \code{sf} or \code{sfc} object containing point geometries representing the ending points.}

\item{travel_mode}{Character. The name of the travel mode to use. See
\code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{default_cutoff}{default \code{NULL}. A numeric scalar. The travel time or distance value at which to stop searching for facilities.}

\item{default_target_destination_count}{default \code{NULL}. An integer scalar. The maximum number of destinations to find per origin.}

\item{output_type}{default \code{NULL}. A scalar character. One of \code{"no_lines"}, \code{"straight_lines"}, \code{"sparse_matrix"}. Controls whether route geometry is returned.}

\item{time_of_day}{default \code{NULL}. A scalar date-time. Either a \code{POSIXt} scalar or a character string parseable by \code{as.POSIXlt()}. The time and date at which travel begins.}

\item{u_turns}{default \code{NULL}. A scalar character. U-turn policy at junctions. One of \code{"allow_backtrack"}, \code{"deadend_intersection"}, \code{"deadend"}, \code{"no_backtrack"}.}

\item{use_hierarchy}{Logical. Whether to use hierarchy when finding routes.
Default: \code{NULL}.}

\item{impedance}{default \code{NULL}. A scalar character. The impedance to minimize. One of \code{"travel_time"}, \code{"minutes"}, \code{"truck_travel_time"}, \code{"truck_minutes"}, \code{"walk_time"}, \code{"miles"}, \code{"kilometers"}.}

\item{accumulate_impedance}{default \code{NULL}. A character vector. Additional impedance values to accumulate.}

\item{restrictions}{Character vector. Restriction names to honor.
Default: \code{NULL}.}

\item{attribute_parameter_values}{default \code{NULL}. A list of objects. Additional values required by an attribute or restriction.}

\item{point_barriers}{default \code{NULL}. An \code{sf} or \code{sfc} object of point geometries representing barriers to restrict or add cost to travel.}

\item{line_barriers}{default \code{NULL}. An \code{sf} or \code{sfc} object of line geometries representing barriers to restrict or add cost to travel.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object. Default: \code{NULL}.}

\item{return_geometry}{default \code{character(0)}. A character vector. Valid values: \code{"origins"}, \code{"destinations"}, \code{"barriers"}, \code{"polyline_barriers"}, \code{"polygon_barriers"}.}

\item{ignore_invalid_locations}{Logical. Whether to ignore invalid locations.
Default: \code{TRUE}.}

\item{return_empty_results}{default \code{FALSE}. A logical scalar. Returns empty results instead of an error on failure.}

\item{geometry_precision}{default \code{NULL}. An integer scalar. Decimal places for x and y values in response geometries.}

\item{locate_settings}{default \code{NULL}. A list controlling how inputs are located on the network.}

\item{crs}{default \code{4326}. The coordinate reference system of the output geometries. Passed to \code{arcgisutils::as_spatial_reference()}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A named list. Elements present depend on \code{return_geometry} and \code{output_type}:
\itemize{
\item \code{od_cost_matrix}: nested cost matrix returned when \code{output_type = "sparse_matrix"}. A list with \code{costAttributeNames} and, per origin ID, a list mapping destination ID to its cost values.
\item \code{od_lines}: OD cost matrix features
\item \code{origins}: origin features
\item \code{destinations}: destination features
\item \code{barriers}: point barrier features
\item \code{polyline_barriers}: polyline barrier features
\item \code{polygon_barriers}: polygon barrier features
\item \code{messages}: status and warning messages from the service
}
}
\description{
Creates an origin-destination cost matrix containing the travel cost between every origin and destination.
}
\examples{
\dontrun{
library(sf)
library(arcgisutils)
set_arc_token(auth_user())

origins <- st_sfc(
  st_point(c(-122.4194, 37.7749)),
  st_point(c(-122.4313, 37.7793)),
  crs = 4326
)

destinations <- st_sfc(
  st_point(c(-122.4083, 37.7858)),
  st_point(c(-122.4000, 37.7900)),
  st_point(c(-122.4561, 37.7513)),
  crs = 4326
)

result <- od_cost_matrix(
  origins = origins,
  destinations = destinations
)

result
}

}
\references{
\href{https://developers.arcgis.com/rest/routing/travelCostMatrix-service-direct/}{API Reference}
}
\seealso{
Other direct: 
\code{\link{find_closest_facilities}()},
\code{\link{find_routes}()},
\code{\link{find_service_areas}()},
\code{\link{route_vehicles}()},
\code{\link{snap_to_roads}()}

Other od: 
\code{\link{download_od_results}()},
\code{\link{od_cost_matrix_job}()}
}
\concept{direct}
\concept{od}
