% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job-last-mile-delivery.R
\name{last_mile_delivery}
\alias{last_mile_delivery}
\title{Solve Last Mile Delivery (Async)}
\usage{
last_mile_delivery(
  orders,
  depots,
  routes = NULL,
  order_specialties = NULL,
  route_specialties = NULL,
  zones = NULL,
  travel_mode = NULL,
  analysis_region = NULL,
  ignore_network_location_fields = FALSE,
  ignore_invalid_order_locations = FALSE,
  earliest_route_start_date = NULL,
  earliest_route_start_time = NULL,
  time_zone_usage_for_time_fields = NULL,
  max_route_total_time = NULL,
  sequence_gap = NULL,
  time_units = NULL,
  distance_units = NULL,
  route_shape = NULL,
  populate_directions = FALSE,
  directions_language = NULL,
  save_route_data = FALSE,
  save_output_network_analysis_layer = FALSE,
  output_format = "feature_set",
  point_barriers = NULL,
  line_barriers = NULL,
  polygon_barriers = NULL,
  token = arcgisutils::arc_token()
)
}
\arguments{
\item{orders}{An \code{sf} object containing point geometries representing
delivery and pickup locations. Use \code{\link[=as_lmd_orders]{as_lmd_orders()}} to prepare with
validated attributes.}

\item{depots}{An \code{sf} or \code{sfc} object containing point geometries
representing depot locations. Prepared with \code{\link[=as_stops]{as_stops()}}.}

\item{routes}{A \code{data.frame} describing vehicle and driver characteristics.
Use \code{\link[=as_lmd_routes]{as_lmd_routes()}} to prepare with validated attributes. Default:
\code{NULL}.}

\item{order_specialties}{A \code{data.frame} mapping orders to the specialties
they require, with columns \code{order_name} and \code{specialty_name}. Default:
\code{NULL}.}

\item{route_specialties}{A \code{data.frame} mapping routes to the specialties
they support, with columns \code{route_name} and \code{specialty_name}. Default:
\code{NULL}.}

\item{zones}{An \code{sf} or \code{sfc} polygon object delineating work territories,
each carrying a \code{name} attribute. Use \code{\link[=as_lmd_zones]{as_lmd_zones()}} to prepare. Default:
\code{NULL}.}

\item{travel_mode}{Character. The name or ID of the travel mode to use.
See \code{\link[=get_travel_modes]{get_travel_modes()}} for available options. Default: \code{NULL}.}

\item{analysis_region}{Character. Region for the analysis. One of:
\code{"europe"}, \code{"japan"}, \code{"korea"}, \code{"middle_east_and_africa"},
\code{"north_america"}, \code{"south_america"}, \code{"south_asia"}, \code{"thailand"}.
Default: \code{NULL} (auto-detected).}

\item{ignore_network_location_fields}{Logical. Whether network location
fields on the inputs are ignored. Default: \code{FALSE}.}

\item{ignore_invalid_order_locations}{Logical. Whether to ignore invalid
orders instead of failing. Default: \code{FALSE}.}

\item{earliest_route_start_date}{Date. Default earliest start date applied to
routes whose \code{earliest_start_date} attribute is missing. Default: \code{NULL}.}

\item{earliest_route_start_time}{An \code{hms} or character \code{"hh:mm:ss"} value
giving the default earliest start time applied to routes whose
\code{earliest_start_time} attribute is missing. Default: \code{NULL}.}

\item{time_zone_usage_for_time_fields}{Character. Time zone used for all
date-time input fields. One of: \code{"geo_local"}, \code{"utc"}. Default: \code{NULL}
(API default: \code{"geo_local"}).}

\item{max_route_total_time}{Numeric. Default maximum allowed total time for
each route, applied to routes whose \code{max_total_time} attribute is missing.
Interpreted in \code{time_units}. Default: \code{NULL}.}

\item{sequence_gap}{Integer. Gap in numerical values to leave in the
\code{Sequence} attribute between adjacent orders when the analysis is solved.
Default: \code{NULL} (API default: \code{1}).}

\item{time_units}{Character. Units for all time-based attribute values. One
of: \code{"seconds"}, \code{"minutes"}, \code{"hours"}, \code{"days"}. Default: \code{NULL}
(API default: \code{"minutes"}).}

\item{distance_units}{Character. Units for all distance-based attribute
values. One of: \code{"miles"}, \code{"kilometers"}, \code{"meters"}, \code{"feet"},
\code{"yards"}, \code{"nautical_miles"}. Default: \code{NULL} (API default: \code{"miles"}).}

\item{route_shape}{Character. Geometry type for output route lines. One of:
\code{"true_shape_with_measures"}, \code{"straight_line"}, \code{"none"}. Default: \code{NULL}
(API default: \code{"straight_line"}).}

\item{populate_directions}{Logical. Whether to generate driving directions
for each route. Default: \code{FALSE}.}

\item{directions_language}{Character. Language code for directions (e.g.
\code{"en"}). Default: \code{NULL}.}

\item{save_route_data}{Logical. Whether to save results as a \code{.zip} file
geodatabase. Default: \code{FALSE}.}

\item{save_output_network_analysis_layer}{Logical. Whether the analysis
settings are saved as a network analysis layer package file. Default:
\code{FALSE}.}

\item{output_format}{Character. Format for output features. One of:
\code{"feature_set"}, \code{"json_file"}, \code{"geojson_file"}. Default:
\code{"feature_set"}.}

\item{point_barriers}{Point barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{line_barriers}{Line barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{polygon_barriers}{Polygon barriers as \code{sf} or \code{sfc} object.
Default: \code{NULL}.}

\item{token}{Authorization token. Default: \code{\link[arcgisutils:token]{arcgisutils::arc_token()}}.}
}
\value{
A \code{last_mile_delivery_job} R6 object inheriting from
\code{arcgisutils::arc_gp_job}. Call \verb{$start()} to submit and \verb{$results} to
retrieve output.
}
\description{
Submits an asynchronous geoprocessing job to solve a last-mile delivery
problem using the ArcGIS \verb{/SolveLastMileDelivery} GP service. Last Mile
Delivery is a specialized vehicle routing problem that creates
geographically clustered delivery routes to minimize fleet operating costs.
}
\references{
\href{https://developers.arcgis.com/rest/routing/last-mile-delivery-service/}{API Reference}
}
